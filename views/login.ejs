<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iniciar Sesión - Batman CRUD</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --batman-black: #0A0E14;
            --batman-red: #B71C1C;
            --batman-dark-red: #7F0000;
            --batman-grey: #4D4D4D;
            --batman-dark-grey: #2D2D2D;
            --gotham-light: #8E9EAB;
        }
    
        body {
            background-color: var(--batman-black);
            color: #fff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
    
        .batman-container {
            background-color: rgba(13, 17, 23, 0.9);
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(183, 28, 28, 0.5);
            padding: 30px;
            margin: 30px auto;
            position: relative;
            overflow: hidden;
            width: 80%;
            max-width: 500px;
        }
    
        .batman-container::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 5px;
            background-color: var(--batman-red);
        }

        .batman-title {
            color: var(--batman-red);
            font-weight: bold;
            text-transform: uppercase;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 5px;
        }

        .batman-subtitle {
            color: var(--batman-grey);
            font-size: 1.2rem;
            margin-top: 0;
            margin-bottom: 20px;
        }
    
        .batman-form {
            background-color: var(--batman-dark-grey);
            padding: 25px;
            border-radius: 10px;
            margin-bottom: 30px;
            border-left: 4px solid var(--batman-red);
        }
    
        .form-label {
            color: var(--batman-red);
            font-weight: 600;
        }
    
        .form-control {
            background-color: var(--batman-grey);
            border: 1px solid #6c757d;
            color: white;
        }
    
        .form-control:focus {
            background-color: var(--batman-dark-grey);
            color: white;
            border-color: var(--batman-red);
            box-shadow: 0 0 0 0.25rem rgba(183, 28, 28, 0.25);
        }
    
        .batman-btn {
            background-color: var(--batman-red);
            color: var(--batman-black);
            border: none;
            font-weight: bold;
            text-transform: uppercase;
            padding: 8px 20px;
            box-shadow: 0 0 10px rgba(183, 28, 28, 0.4);
            transition: all 0.3s ease;
            width: 100%;
            margin-top: 10px;
        }
    
        .batman-btn:hover {
            background-color: #da3232;
            box-shadow: 0 0 15px#da3232;
            transform: translateY(-2px);
        }
    
        .batman-alert {
            background-color: #300;
            color: white;
            border-color: #900;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(255, 0, 0, 0.3);
        }
    
        .batman-alert-heading {
            color: var(--batman-red);
        }
    
        .gotham-backdrop {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://preview.redd.it/cciryoim2wt71.jpg?auto=webp&s=cabb31d7907a96c11fb40e75a0cd9af7b17c9c01') no-repeat center center;
            background-size: cover;
            opacity: 0.2;
            z-index: -1;
        }

        .register-link {
            text-align: center;
            margin-top: 15px;
            color: var(--gotham-light);
        }

        .register-link a {
            color: var(--batman-red);
            text-decoration: none;
        }

        .register-link a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="gotham-backdrop"></div>
    <div class="container batman-container">
        <div class="batman-header text-center">
            <h2 class="batman-title">INICIAR SESIÓN</h2>
            <p class="batman-subtitle">Noyola Gómez Emilio Damian</p>
        </div>
        
        <% if (locals.errores && errores && errores.length > 0) { %>
            <div class="alert batman-alert">
                <h4 class="alert-heading batman-alert-heading">Error al iniciar sesión:</h4>
                <ul>
                    <% errores.forEach(error => { %>
                        <li><%= error %></li>
                    <% }); %>
                </ul>
            </div>
        <% } %>
        
        <form action="/login" method="post" class="batman-form">
            <div class="mb-3">
                <label for="email" class="form-label">Email:</label>
                <input type="email" name="email" id="email" class="form-control" value="<%= locals.datos ? datos.email : '' %>" required>
            </div>
            <div class="mb-3">
                <label for="password" class="form-label">Contraseña:</label>
                <input type="password" name="password" id="password" class="form-control" required>
            </div>
            <button type="submit" class="btn batman-btn">
                <i class="fas fa-sign-in-alt me-2"></i>Iniciar Sesión
            </button>
        </form>
        
        <div class="register-link">
            ¿No tienes una cuenta? <a href="/registro">Regístrate aquí</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>